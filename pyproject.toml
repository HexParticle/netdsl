[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "netdsl"
version = "0.1.0"
dependencies = [
    "ply"
]

[tool.setuptools.packages.find]
where = ["."]